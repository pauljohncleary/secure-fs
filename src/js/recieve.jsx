var React = require('react');

module.exports = React.createClass({

  componentDidMount() {

    //get the hash from the url?
    //connect to webrtc

  },

  render: function() {
    return (
      <h2>recieve page</h2>
    );
  }
});
